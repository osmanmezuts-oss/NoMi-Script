name = "nomi-api-worker"
main = "src/index.js"
compatibility_date = "2025-08-01"

# ====== BINDINGS (crear en Cloudflare, NO incluir valores reales) ======
# Secretos (wrangler secret put):
#   GROQ_API_KEY        -> API key de Groq
#   ADMIN_SECRET        -> token para endpoints administrativos privados
#   ACCESS_TOKEN_SECRET -> pepper HMAC para hashes de códigos/tokens de acceso
#   TAVILY_API_KEY      -> API key de Tavily; REQUERIDA solo para la herramienta
#                          de búsqueda web NoMi (`herramienta.tipo = "busqueda"`).
#                          Nunca se expone al cliente ni se acepta del cliente.
#                          Sin ella, la búsqueda devuelve fallo_proveedor con
#                          rollback de cupo; el resto (chat, clima) no la usa.

# D1: base de datos de usuarios, invitaciones, cuota mensual y contadores.
# Crear con: wrangler d1 create nomi-api-db  (anotar el database_id)
[[d1_databases]]
binding = "NOMI_DB"
database_name = "nomi-api-db"
database_id = "e7bfa3b1-af8d-4406-91e5-dfdd0b63ecac"

# Durable Object para límites globales y bolsa de créditos (no se confía en contadores locales).
[durable_objects]
bindings = [{ name = "RATE_LIMITER", class_name = "RateLimiterDO" }]

[[migrations]]
tag = "v1"
new_sqlite_classes = ["RateLimiterDO"]

[vars]
NOMI_API_VERSION = "0.1.0"
# Opcional (P2): timeout en milisegundos de la llamada a Tavily en la búsqueda
# web NoMi. Default 10000 (10 s); vencido -> fallo_proveedor con rollback de cupo.
# TAVILY_TIMEOUT_MS = 10000